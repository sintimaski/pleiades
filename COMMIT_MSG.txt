Production readiness: validation, CLI, tests, and engine error handling

- Validation: accept str|Path in validate_catalog_schema and
  validate_prepartitioned_dir; add Args to docstrings
- CLI: catch FileNotFoundError, OSError, ValueError and exit 1 with
  "pleiades: <message>" on stderr instead of traceback
- Tests: use tmp_path instead of /tmp in unit tests (portability);
  add CLI tests for exit 1 and stderr on missing catalog and invalid radius;
  add pytest-timeout and 60s timeout for integration tests
- merge_match_to_catalog: document that only how='left' is supported;
  emit UserWarning when how is not 'left'; add test for warning
- cross_match: emit DeprecationWarning when Rust returns None (fallback
  stats-from-Parquet path)
- Rust (lib.rs): map engine errors to Python exceptions—raise
  PyFileNotFoundError when io::ErrorKind::NotFound, else PyOSError
